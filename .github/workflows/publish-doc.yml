name: Publish Documentation

on:
  push:
    branches:
      - 'main'

jobs:

  tests:
    name: Publish Documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.8
      - run: pip install tox
